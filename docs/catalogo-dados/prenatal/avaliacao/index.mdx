---
title: Avaliacao
---

import Figura from '@site/src/components/Figura';
import TabelaMapeamento from '@site/src/components/TabelaMapeamento';
import problemaAtendimentoData from './data/problemaAtendimento.data';
import preNatalData from './data/preNatal.data';
import alergiasData from './data/alergias.data';

# 5. Avaliação

Para apoiar a compreensão das funcionalidades do módulo de **Avaliação**, disponibilizamos um vídeo introdutório e de rápida visualização. Esse material apresenta, diretamente a partir das telas do sistema, as principais características e passos do módulo, oferecendo ao usuário uma visão prática e imediata de seu funcionamento.

### 5.1 Mapeamento Técnico dos Campos do Sistema

As tabelas a seguir apresentam o mapeamento dos campos da área de Avaliação, indicando sua origem no banco de dados do PEC. Para cada tabela, são disponibilizadas imagens extraídas diretamente da interface do sistema, com o objetivo de facilitar a identificação visual dos campos na plataforma.

### 5.2 Problemas e Condições Avaliados

<Figura
  src="/img/prenatal/tela_11_avaliacao_problema_atendimento.png"
  alt="Problemas e condições avaliados"
  numero={8}
  descricao="Tela de Problemas e Condições Avaliados no PEC"
  fonte="Prontuário Eletrônico - Versão 5.5.6"
/>

{problemaAtendimentoData.secoes.map((secao, index) => (
  <TabelaMapeamento
    key={index}
    numero={`8${String.fromCharCode(97 + index)}`}
    titulo={secao.titulo}
    linhas={secao.linhas}
    fonte="DIAna SAPS"
  />
))}

### 5.3 Pré-natal

<Figura
  src="/img/prenatal/tela_12_avaliacao_pre_natal.png"
  alt="Pré-natal"
  numero={9}
  descricao="Tela de Pré-natal no PEC"
  fonte="Prontuário Eletrônico - Versão 5.5.6"
/>

{preNatalData.secoes.map((secao, index) => (
  <TabelaMapeamento
    key={index}
    numero={`9${String.fromCharCode(97 + index)}`}
    titulo={secao.titulo}
    linhas={secao.linhas}
    fonte="DIAna SAPS"
  />
))}

### 5.4 Alergias

<Figura
  src="/img/prenatal/tela_13_avaliacao_alergias.png"
  alt="Alergias"
  numero={10}
  descricao="Tela de Alergias no PEC"
  fonte="Prontuário Eletrônico - Versão 5.5.6"
/>

{alergiasData.secoes.map((secao, index) => (
  <TabelaMapeamento
    key={index}
    numero={`10${String.fromCharCode(97 + index)}`}
    titulo={secao.titulo}
    linhas={secao.linhas}
    fonte="DIAna SAPS"
  />
))}

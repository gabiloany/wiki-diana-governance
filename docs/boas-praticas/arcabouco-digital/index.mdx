---
title: Referências
---

import { referencias } from '@site/docs/boas-praticas/arcabouco-digital/data/referencias.data';

# Referências

<ol>
  {referencias.map((r, i) => (
    <li id={`ref-${r.id}`} key={r.id}>{r.texto}</li>
  ))}
</ol>